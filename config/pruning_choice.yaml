pruning_choice:
  target_layer: null
  strategy: cosine_anchor
  anchor_dataset_tag: "dataset"
  anchor_file_suffix: ""
  clustered:
    use: true
    keep_top_cluster_ratio: 0.3

  unclustered:
    use: true
    bottom_x_percent: 50.0
    variance_lock_top_n: 10
