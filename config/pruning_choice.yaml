pruning_choice:
  target_layer: null
  strategy: count_based  # cosine_anchor | count_based

  # run_sequential: true  # только для strategy=cosine_anchor:
  #   шаг 1 — удаляет нижние N% по загруженности (count_based, от полного num_experts),
  #   шаг 2 — cosine_anchor только по оставшимся после шага 1.

  percent_mode: global
  global_removal_percent: 0.3
  per_layer_removal_percent: {}

  anchor_dataset_tag: "dataset"
  # anchor_file_suffix: ""
  # clustered:
  #   use: true
  #   keep_top_cluster_ratio: 0.3

  # unclustered:
  #   use: true
  #   bottom_x_percent: 50.0
  #   variance_lock_top_n: 10
